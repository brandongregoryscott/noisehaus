const DELETE_BOARD_ROUTE = "/boards/:slug";

const UPDATE_BOARD_ROUTE = "/boards/:slug";

const CREATE_BOARD_ROUTE = "/boards";

const LIST_BOARD_ROUTE = "/boards";

const GET_BOARD_ROUTE = "/boards/:slug";

const CREATE_BOARD_FILE_ROUTE = "/boards/:slug/files";

const GET_BOARD_FILE_ROUTE = "/boards/:slug/files/:id";

const UPDATE_BOARD_FILE_ROUTE = "/boards/:slug/files/:id";

const LIST_BOARD_FILE_ROUTE = "/boards/:slug/files";

const DELETE_BOARD_FILE_ROUTE = "/boards/:slug/files/:id";

const GET_BOARD_FILE_SIZE_ROUTE = "/boards/:slug/files/size";

const CREATE_FEEDBACK_ROUTE = "/feedback";

export {
    CREATE_BOARD_FILE_ROUTE,
    CREATE_BOARD_ROUTE,
    CREATE_FEEDBACK_ROUTE,
    DELETE_BOARD_FILE_ROUTE,
    DELETE_BOARD_ROUTE,
    GET_BOARD_FILE_ROUTE,
    GET_BOARD_FILE_SIZE_ROUTE,
    GET_BOARD_ROUTE,
    LIST_BOARD_FILE_ROUTE,
    LIST_BOARD_ROUTE,
    UPDATE_BOARD_FILE_ROUTE,
    UPDATE_BOARD_ROUTE,
};
